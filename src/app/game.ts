export interface Game {
    id: string;
    citiesCount: string;
    playerName: string;
    gameTime: string;
    rounds: Array<string>;
    quizMinutes: string;
    score: number;
    quizOptions: Array<string>;

}